# ==================================
# WhatsApp Web Multi-Channel Platform
# Environment Variables Template
# ==================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Fill in your actual values in .env.local
# 3. Never commit .env.local (already in .gitignore)
#

# ----------------------------------
# Supabase Configuration
# ----------------------------------
# Get these from: https://app.supabase.com/project/[your-project]/settings/api

# Public URL of your Supabase project
# Format: https://[project-id].supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anonymous (public) key - safe to expose in browser
# This key respects Row Level Security policies
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# Service role key - NEVER expose to client, server-side only
# This key bypasses Row Level Security - use with caution
# Used for: Admin operations, webhook processing, cron jobs
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key-here

# ----------------------------------
# Security & Encryption
# ----------------------------------

# Encryption key for storing Whapi tokens (32-byte random string)
# Generate with: openssl rand -base64 32
# CRITICAL: Keep this secret. If lost, existing channel tokens cannot be decrypted.
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# Secret for verifying cron job requests from Vercel
# Generate with: openssl rand -base64 32
# Used to prevent unauthorized access to /api/cron/* endpoints
CRON_SECRET=your-cron-secret-here

# ----------------------------------
# Application Configuration
# ----------------------------------

# Public URL of your application
# Development: http://localhost:3000
# Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------------------------
# Optional: Whapi.cloud Testing
# ----------------------------------
# These are optional and only needed if you want to test with a real Whapi account
# during development. In production, these are stored in the database per channel.

# Whapi API Base URL (default: https://gate.whapi.cloud)
# WHAPI_API_BASE_URL=https://gate.whapi.cloud

# Test channel token (for development/testing only)
# Get from: https://whapi.cloud/dashboard
# WHAPI_TEST_TOKEN=your-test-token-here
